#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;
const int MAXA = 100;
signed main(int argc, char* argv[]) {
	registerValidation(argc, argv);
	inf.readInt(1, MAXA, "a");
	inf.readEoln();
	inf.readInt(1, MAXA, "b");
	inf.readEoln();
	inf.readEof();
}
